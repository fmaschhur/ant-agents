import node
import edge

class Graph(object):

    def init(self, x, y, nest):
        self.nodes = self.create_nodes(x, y)
        self.edges =
        self.nest = nest
        self.antcount = 0

    def create_nodes(self, x, y):
